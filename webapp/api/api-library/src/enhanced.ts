
// ⚠️ File generato automaticamente — non modificare a mano

import {slotGuardServiceApiBase} from './slotGuard/slotGuardApi';

export const slotGuardServiceApi = slotGuardServiceApiBase.enhanceEndpoints({
  addTagTypes: ['slotGuard'],

});
