const useUserHook = () => {
  /* const [getUserInfo,{data:userInfoResponse,isLoading:userInfoIsLoading}]= msAuthServiceApi.useLazyGetUserInfoByJwtQuery();
    const {isLogged} =useAuthHook()
    useEffect(() => {
        if(isLogged){
            getUserInfo();
        }
    },[isLogged])

    return {userInfoResponse,isLoading:userInfoIsLoading}*/
};
export default useUserHook;
